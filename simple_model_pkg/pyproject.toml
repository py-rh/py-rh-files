[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-model-pkg"
version = "0.1.0"
description = "Simple neural network model"
requires-python = ">=3.8"
dependencies = [
    "torch",
]

[tool.setuptools]
py-modules = ["model", "trainer"]
